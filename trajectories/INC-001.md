# TraceRoot --- INC-001 Agent Trajectory

This document is a representative execution trace for the TraceRoot
investigation agent. It follows the agent from its initial objective
through tool selection, tool results, hypothesis formation,
verification, and the final root-cause report.

## Incident

**Incident:** INC-001\
**Objective:** Determine why `POST /orders` intermittently returns HTTP
500 under increased request volume, identify the responsible source
location, explain the failure mechanism, and determine the minimal
corrective action.

Initial incident evidence included:

-   `GET /health` continues to return HTTP 200.
-   `POST /orders` intermittently returns HTTP 500.
-   Failures become visible under increased request volume.
-   The application reports database-related errors.
-   The service remains running while requests begin failing.
-   Production logs contain `Connection pool exhausted`.
-   The stack trace points through `ConnectionPool.connect()` and
    `createOrder()`.

------------------------------------------------------------------------

## Initial Investigation State

``` json
{
  "incidentId": "INC-001",
  "repositoryPath": "incidents/INC-001/repository",
  "evidence": [],
  "hypotheses": [],
  "actions": [],
  "status": "initialized"
}
```

### Agent objective

The investigation starts by locating the database connection acquisition
path. This is a targeted repository search rather than immediately
asserting a root cause.

------------------------------------------------------------------------

# Step 1 --- Locate Connection Acquisition

### Agent action

**Tool:** `search_repository`

**Input:**

``` json
{
  "repositoryPath": "incidents/INC-001/repository",
  "query": "pool.connect"
}
```

**Reason:**

> Locate database connection acquisition in the repository.

### Tool result

The repository search returned:

``` text
incidents/INC-001/repository/src/orders/service.js:4
const connection = await pool.connect();

incidents/INC-001/repository/src/orders/service.ts:15
const connection = await pool.connect();
```

### Evidence added

``` text
src/orders/service.js:4
const connection = await pool.connect();

src/orders/service.ts:15
const connection = await pool.connect();
```

### Agent interpretation

`createOrder` acquires a database connection. The incident already
reports database-related failures under increased request volume, so the
next useful question is whether the acquired connection is released.

### Next objective

Determine whether the connection acquisition is paired with
`connection.release()`.

------------------------------------------------------------------------

# Step 2 --- Check Connection Release

### Agent action

**Tool:** `search_repository`

**Input:**

``` json
{
  "repositoryPath": "incidents/INC-001/repository",
  "query": "connection.release"
}
```

**Reason:**

> Check whether acquired database connections are released.

### Tool result

The repository fixture contains the pool's release implementation, but
the incident's `createOrder` path does not restore the acquired
connection.

Relevant repository evidence includes:

``` text
src/orders/service.ts:15
const connection = await pool.connect();
```

and the pool implementation exposes a release operation.

The fixture's intentionally faulty `createOrder` implementation has the
release call removed/commented out in the `finally` path.

### Agent interpretation

The application has a bounded connection pool and an order handler that
acquires a connection without releasing it. This matches the production
symptom much better than a whole-service outage.

The agent therefore forms competing hypotheses rather than treating the
first observation as proof.

------------------------------------------------------------------------

# Hypothesis Generation

## Hypothesis 1 --- High confidence

**Statement:**

> Database connection leak caused by failing to release acquired
> connections.

**Supporting evidence:**

1.  `createOrder` acquires a database connection with `pool.connect()`.
2.  The connection pool has a maximum capacity of 5.
3.  Production logs report `Connection pool exhausted`.

**Confidence:** 90%

## Hypothesis 2 --- Alternative

**Statement:**

> The entire service or application process is unavailable.

**Contradicting evidence:**

1.  `GET /health` continues to return HTTP 200.
2.  The application process remains running.

**Confidence:** 10%

### Decision

The highest-confidence hypothesis is the database connection leak, but
the agent does not stop at hypothesis generation. It invokes
verification.

------------------------------------------------------------------------

# Step 3 --- Verify the Highest-Confidence Hypothesis

### Agent action

**Tool:** `verify_hypothesis`

**Input:**

``` json
{
  "repositoryPath": "incidents/INC-001/repository",
  "hypothesis": {
    "statement": "Database connection leak caused by failing to release acquired connections.",
    "confidence": 0.9
  }
}
```

**Reason:**

> Verify the highest-confidence hypothesis against the repository
> fixture.

### Verification behavior

The verification step runs the fixture regression tests against the
intentionally faulty repository.

The fixture reproduces the connection-pool exhaustion behavior caused by
leaked connections.

The verification output records:

``` text
status: confirmed
method: Fixture regression tests
```

The underlying fixture test command is expected to fail while the faulty
repository is present; that failure is the reproduction signal used to
confirm the hypothesis.

### Agent interpretation

The observed failure is reproducible in the supplied fixture:

``` text
pool.connect()
    ↓
connection is not released
    ↓
active connections accumulate
    ↓
pool reaches max capacity (5)
    ↓
Connection pool exhausted
    ↓
POST /orders returns HTTP 500
```

The high-confidence hypothesis is therefore confirmed.

------------------------------------------------------------------------

# Final Investigation State

``` text
Incident:        INC-001
Status:          confirmed
Evidence items:  15
Hypotheses:      2
Tool actions:    3
Confidence:      90%
```

### Agent action trace

``` text
1. search_repository
2. search_repository
3. verify_hypothesis
```

------------------------------------------------------------------------

# Final Root-Cause Report

## Root cause

**Database connection leak caused by failing to release acquired
connections.**

## Exact location

``` text
src/orders/service.ts
function: createOrder
```

## Mechanism

`createOrder` acquires a database connection with `pool.connect()` but
does not release it after the order operation completes.

## Causal chain

1.  `createOrder` acquires a database connection from the connection
    pool.
2.  The acquired connection is not released after the order operation
    completes.
3.  Active database connections accumulate across requests.
4.  The connection pool has a maximum capacity of 5 active connections.
5.  Once the pool reaches capacity, subsequent order requests fail with
    `Connection pool exhausted`.
6.  The server converts the resulting exception into an HTTP 500
    response.

## Remediation

Restore:

``` text
connection.release()
```

in the `finally` block of `createOrder`.

------------------------------------------------------------------------

# Why the trajectory matters

The agent does not simply map an incident description directly to a
root-cause sentence.

The trajectory shows the progression:

``` text
Incident symptoms
      ↓
Targeted repository search
      ↓
Connection acquisition evidence
      ↓
Connection-release investigation
      ↓
Competing hypotheses
      ↓
Highest-confidence hypothesis
      ↓
Fixture verification
      ↓
Confirmed root cause
      ↓
Exact source location + remediation
```

This is the intended evidence-driven behavior of TraceRoot: each
investigation action has an objective, tool response becomes evidence,
evidence changes the investigation state, and verification is required
before the final report is marked confirmed.
