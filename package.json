{
  "name": "traceroot-agent",
  "version": "0.1.0",
  "description": "Evidence-driven agentic workflow for production incident investigation.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/**/*.test.ts",
    "baseline": "tsx src/baseline/run.ts",
    "agent": "tsx src/agent/run.ts",
    "evaluate": "tsx src/evaluator/run.ts",
    "fixture:test": "cd incidents/INC-001/repository && npm test"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "tsx": "^4.23.12",
    "typescript": "^7.0.2"
  }
}
